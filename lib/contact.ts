// The address a visitor can actually reach a person on. Single source, so the
// contact link and anything that emails later cannot drift apart.
export const CONTACT_EMAIL = 'midsesh.social@gmail.com';
