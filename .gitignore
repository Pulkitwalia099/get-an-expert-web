node_modules/
.next/
out/
build/
.vercel/
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.env.*.local
.DS_Store
npm-debug.log*

# eval run reports
evals/.last-run.json
.gstack/

# Internal notes kept out of a public repo.
docs/handoff.md
.env*

# Supabase CLI link cache. Holds a pooler connection string with credentials.
supabase/.temp/

# Agent worktrees. They are real git worktrees living inside the repo, so
# without this a plain `git add -A` stages them as embedded gitlinks.
.claude/worktrees/
